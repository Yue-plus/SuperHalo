import 'package:flutter/material.dart';

class Tourist extends StatelessWidget {
  const Tourist({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("只是看看", textDirection: TextDirection.ltr,),
    );
  }
}